# Postgres container credentials (used by docker-compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=chokepoint
# Host-side port for the compose Postgres.
# Defaults to 15432 so it does not clash with a locally installed Postgres on 5432
# or other Docker projects; inside the Docker network the service still uses 5432.
POSTGRES_PORT=15432

# SQLAlchemy URL used by the app, alembic, and tests when run from your host machine.
# Inside the app container the host is `db` on port 5432 (set by docker-compose).
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:15432/chokepoint

# --- Ingestion (M1) ---

# Required for the EIA crude spot price source. Register free at
# https://www.eia.gov/opendata/register.php
EIA_API_KEY=

# GDELT DOC API article list size cap (API allows up to 250)
GDELT_MAX_RECORDS=75

# HTTP client behavior for all ingestion sources
HTTP_TIMEOUT_SECONDS=30
RETRY_DELAY_SECONDS=2

# Milestone M1 scope: every signal is tagged with this corridor/commodity
INGEST_CORRIDOR=hormuz
INGEST_COMMODITY=crude_oil

# --- Extraction & scoring (M2) ---

# Required for LLM extraction of GDELT articles. Create a key at
# https://console.anthropic.com/ (Haiku pricing ~$0.05-0.15 per daily run)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5

# EIA volatility baseline: severity = |day-over-day % change| / trailing
# median |% change| over this many prior points, times the scale factor
EIA_BASELINE_DAYS=30
EIA_VOL_SCALE=1.0
